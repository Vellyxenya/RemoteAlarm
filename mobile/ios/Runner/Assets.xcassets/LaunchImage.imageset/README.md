# Launch Screen Assets

Customize the launch screen with desired assets by replacing the image files in this directory.

Alternatively, update assets by opening the Xcode project with `open ios/Runner.xcworkspace`, selecting `Runner/Assets.xcassets` in the Project Navigator and dropping in the desired images.